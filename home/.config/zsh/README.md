# zsh

The execution order of the startup files is
`.zshenv → .zprofile → .zshrc → .zlogin → .zlogout`

`.zprofile` is for login shells, `.zshrc` is for interactive shells.
Read the full answer on [Unix StackExchange](https://unix.stackexchange.com/a/71258).

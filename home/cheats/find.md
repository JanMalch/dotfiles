# Finding stuff

- `fzf` for fuzzy finding
- `fd` instead of `find`
- `rg` (ripgrep) to respect `.gitignore`
  - `rg -l` to only list files
  - `rg -tzsh` to filter by file type, see also `rg --type-list` 
  - `rg --glob '*.kt'` to filter by glob

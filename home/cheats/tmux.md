# tmux

Everything is prefixed by leader `<C-a>`
- `d` to detach (session remains active)
- panes
  - `s` for horizontal split
  - `v` for vertical split
  - `x` to kill
- navigate
  - with vim motions (`h`, `j`, `k`, `l`) or arrows
- `:list-keys`

